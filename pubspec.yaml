name: flutter_bloc_and_validation
description: Pryecto de ejemplo.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.0.1
  equatable: ^2.0.0
  rxdart: ^0.26.0
  http: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable: ^4.1.2

flutter:

  uses-material-design: true
